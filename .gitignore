.evidence/runs/
__pycache__/
*.pyc

# per-machine headless-loop permission config (operator-seeded, never committed)
opencode.jsonc

# local board service state (scripts/jig-board.sh) — pid, log and run snapshot
.jig/
