T1: add ralph_log_selftest_push to scripts/ralph-log.sh and call it from the first-green selftest block in scripts/ralph-build.sh — select the current run_id's rows out of .evidence/selftest-<spec>.jsonl, drop the diff from KILLED rows and clip it on the rest, POST as artifact kind `selftest`; unset HARNESS_REPORT_URL pushes nothing and prints nothing, a failed push never fails the run
T2: implement GET /api/artifact?key=&name= in scripts/dispatch/coordinator.py, replacing the 400 stub and the dead /artifact/ branch above it — 200 text/plain with the stored bytes, 400 naming a missing parameter, 404 for an unknown run or artifact WITHOUT calling _touch, and no token required (reads are open, matching /api/runs)
T3: add the mutant panel and artifact viewer to scripts/dispatch/board.html — fetch a selected attempt's `selftest` artifact via /api/artifact, render killed/survivor/wrong-reason/hung counts and per-mutant rows survivors-first with assertion id, target, WHY and the install-time diff, every worker-supplied field inserted with textContent, unparseable lines skipped
T4: write scripts/jig-board.sh (start|stop|status|url on COORD_PORT with a .jig/ pidfile, python3-only, no Docker), add the --board flag to scripts/run-loop.sh that brings it up and exports HARNESS_REPORT_URL before the phases, gitignore .jig/, and document both in docs/coordinator.md — no flag means no process started and HARNESS_REPORT_URL untouched
