T1: the loop proves a gate can fail at the moment its green would buy a commit. When a task's gate FIRST goes green (v1.1 — selftest measures a gate against BUILT work; pristine, a red-first gate fails everything and every mutant dies vacuously, so before-attempt-1 proves nothing), ralph-build.sh runs gate-selftest.sh on that task's mutants/ corpus (hermetic working-tree copy — the tool's own mechanism). Only all-KILLED commits; any SURVIVOR, WRONG-REASON, HUNG, or uncovered-assertion-id STOPs the run with exit 6 BEFORE the commit, the built tree left in place, and wording that says plainly the GATE failed its selftest — a refusal must never read as the work failing the gate. Once per task at first green, not per attempt — 20260831r T5 freezes gate and corpus for the run's duration, so re-proving proves nothing. SELFTEST_EVID defaults to the worked repo's .evidence/ so every dogfood run appends kill/survivor rows (20260830f seam unchanged) — the research dataset accumulates as a side effect. Verdict logic stays in gate-selftest.sh (20260831a §4: enumerate, delegate, never re-implement); the loop owns only enumeration and refusal. Supersedes 20260828k outcome 8 on ratification; scope is the run's own spec in the run's own worktree, NEVER a repo-wide sweep — past specs' corpora are history with their gates.
T2: a new spec without a corpus is refused; a legacy spec is warned out loud. Per OQ1's ratified policy: a new-shape spec (dated 20260831u or later) whose task carries no mutants/ directory — or an empty one — is refused at the same preflight, naming the task, the monolithic-refusal way (20260831p: structural defect, no override hatch); a pre-existing spec with no corpus runs with a single loud warn line naming what is unproven, never a silent skip, and is NEVER backfilled — editing rigor into history is the same defect as editing history's gates. The boundary is the spec's date prefix, which is already the repo's own ordering; the gate must show both sides: a fixture new spec without mutants/ is refused, the same fixture dated pre-u proceeds warned, and the warn text appears exactly once.
T3: authoring mutants is the default path, not a memory. new-spec.sh scaffolds, beside each task's stub verify.sh, a mutants/ directory holding one template mutant whose MUTANT:/TARGET:/WHY: header parses under gate-selftest.sh's contract (header present and well-formed; the body is a placeholder the author must replace — a scaffold that passes selftest for free would be a lie). new-spec.sh --check fails a spec any of whose tasks lacks a well-formed mutant and passes a fresh scaffold's shape check while still failing its red-by-construction gates. specs/TEMPLATE.md §11 gains the one-paragraph authoring move where the per-task gate shape is already taught: one mutant per assertion id, authored by INVERTING the assertion — write the plausible wrong implementation that satisfies a lazy reading — before the build starts, because a convention that lives only in the spec that ratified it loses to the template every author copies from (amendments, 2026-08-31, twice now).
