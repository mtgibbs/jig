#!/usr/bin/env bash
# Gate for T01-preflight-selftest (20260831u-mutants-always-run). RED BY CONSTRUCTION: this stub fails until it is
# replaced with the task's real acceptance checks. Author the checks FIRST, run them red,
# capture evidence/red-before-green.txt, and only then build the deliverable.
set -uo pipefail
echo "  FAIL  T01-preflight-selftest: gate is an unwritten stub — author this task's acceptance checks" >&2
exit 1
