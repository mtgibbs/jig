T3: give the status file the same host level in scripts/ralph-status.sh. HB_DIR's per-spec path gains `<host>` between the spec slug and the file, so the contract becomes `$RALPH_STATUS_DIR/<spec-slug>/<host>/<agent>-<pid>.json` — the file basename stays `<agent>-<pid>.json`, unchanged, for the same reason T2's leaf does. Use scripts/run-key.sh, resolved the same way T2 resolves it. Update the contract comment at ralph-status.sh:9 to match, because that comment is the only written record of this path. Do NOT change the `"pid":%s` field or quote it: it stays an unquoted JSON number, and this spec deliberately does not make the pid non-numeric.

T4: record the fleet identity in log_meta, in scripts/ralph-log.sh. Add exactly two fields to the per-attempt JSON object of specs/evidence-replayable section 3.2: `host`, the discriminator, and `run_key`, the string `<host>/<agent>-<pid>`. Both are always strings and never null — the resolver always yields a value, so there is no not-measurable case here and the null-vs-0 discipline does not apply. Pass both through `jq --arg` like every other value; never interpolate into the jq program. Leave `run_id` exactly as it is, still the bare leaf: it is what the existing readers join on, and this task adds an identity rather than replacing one. Every existing field keeps its current name, type and value.

T5: teach the readers the new level, in scripts/loop-doctor.sh and scripts/loop-index.py. Both walk the runs tree and recognise a run by matching its basename against `[a-z0-9]*-[0-9]*` (loop-doctor.sh:94 and :100) or by splitting it (loop-index.py:335). A host directory now sits between the spec slug and the run, and a Kubernetes pod name such as `harness-run-7` MATCHES that glob — so the host level must be excluded by its POSITION in the tree, never by trying to tell the two names apart. A run directory holding only `.log` and `.diff` files must still classify exactly as it does today, `unparsed` must still be 0 on a healthy run, and the run count must not change: the same corpus at the new depth must produce the same numbers as before, and that equality is the positive control. loop-index.py's `agent, pid = name.split("-", 1)` still applies to the leaf and must keep working unchanged.
