T4: add the per-attempt metadata writer to scripts/ralph-log.sh. Define `log_meta` writing `log_path <task> <attempt> json` as one JSON object with exactly the fields in spec.md section 3.2. Emit it with `jq -n` passing EVERY value via `--arg`/`--argjson` — never interpolate a shell variable into the jq program, and never build the JSON with printf; a task label or binding path containing a quote would produce an unparseable record. `null` and `0` are different values: `verify_rc` is null when the gate never ran, `bytes_patch` is null when no patch was written, and neither may be coerced to 0. Sizes come from `wc -c` (POSIX, identical on macOS and Linux) — do NOT use `stat`, whose `-f` flag means filesystem-status on Linux and returns 0 with garbage. If jq is absent, write nothing and report once on stderr. Its signature is EXACTLY `log_meta <task-label> <attempt>` — two positional parameters and no more; the gate calls it as `log_meta T1 1` and nothing else is passed. Every other field in section 3.2 comes from the environment or from the caller's shell scope (ralph-log.sh is sourced INTO ralph-build.sh, so the loop's own variables are already visible): `RUN_LABEL`, `RALPH_AGENT`, `RALPH_EXEC_CMD`, `ROOT`, `SPEC_DIR`, `LOG_DIR`, and the attempt's timing/rc variables. Every one of them may be unset — read each through a `${VAR:-default}` expansion so an unset variable is a null or a default, never an error, and emit the field regardless: the gate asserts that all seventeen keys are PRESENT. Like every writer here it must exit 0 no matter what: guard on LOG_OK, wrap the write `|| true`, and never let an unwritable LOG_DIR return nonzero — the gate probes exactly that. No call site: do NOT touch ralph-build.sh or ralph-judge.sh in this task. T7 owns every call site, and adding one here both violates scope and invents a signature the gate does not use.

T5: make a run addressable, in scripts/ralph-log.sh log_init. After LOG_DIR is resolved and created, maintain a symlink named `latest` in LOG_DIR's PARENT directory pointing at LOG_DIR's basename — a RELATIVE target, because an absolute one breaks the moment the repo is cloned or the container that wrote it is gone. Replace any existing `latest` atomically (`ln -sfn`), and never fail the loop if the filesystem has no symlink support. LOG_DIR's own basename stays exactly `<agent>-<pid>` — do not fold a label, slug or timestamp into it, because every reader parses the pid out of that leaf. Also read the optional `RUN_LABEL` environment variable and expose it for T4's record, defaulting to null (a JSON null, not the empty string) when unset.

T6: teach scripts/loop-doctor.sh the new artifacts. Its per-file `case` at loop-doctor.sh:142 counts anything unrecognised as `unparsed`, so the four artifacts T1-T4 add would make every HEALTHY run report drift. Add arms for `T*-attempt*.prompt.md`, `T*-attempt*.gate.txt`, `T*-attempt*.patch` and `T*-attempt*.json`, placed ABOVE the existing `T*-attempt*.log` arm so nothing shadows them, and skip a `latest` symlink rather than treating it as a run. The `attempts` counter must still equal the number of `.log` transcripts only, and `bytes_last` must still measure the highest-numbered transcript. A run directory holding only `.log` and `.diff` files must classify exactly as it does today — the reader change is additive, and that legacy case is the positive control.

T7: wire every call site, and this task is LAST because it edits the script the loop is executing. In scripts/ralph-build.sh: call log_prompt immediately BEFORE the `run_bounded "$EXEC_TIMEOUT" $RALPH_EXEC_CMD` line (ralph-build.sh:152) so a stillborn executor still leaves the input that killed it; call log_gate on BOTH the passing branch and the failing branch, reusing the `$out` already captured at ralph-build.sh:189 — verify.sh must run exactly as many times as it does today; call log_patch on the passing branch BEFORE the existing `git -C "$ROOT" add -A` at ralph-build.sh:192; call log_meta as the last statement of every attempt path including the no-op and stillborn exits. In scripts/ralph-judge.sh, persist the judge's prompt through log_prompt with the same naming. Bash reads a running script by byte offset, so this edit will break the in-flight process: expect a nonzero exit after this task, check `bash -n` on the file before treating it as a defect, and re-invoke the loop rather than resuming it.
