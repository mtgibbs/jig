T1: create docker/loop-executor.VERSION containing exactly one line, the bare semver `0.1.0` with no `v` prefix and no other text. It is the image tag source, and it matches the convention the review-hub image already uses so a Flux ImagePolicy written against one reads the other. Create only this file in this task.

T2: create .github/workflows/build-images.yml. This repository has no .github directory yet, so create it. The workflow builds and pushes container images to GHCR, and it is modelled on a known-good workflow that does exactly this job in this environment — described in full here, since it lives in another repository you cannot read. It triggers on `push` to the `main` branch, filtered by `paths` so an unrelated commit does not rebuild: the list must include `docker/**` and the workflow's own path `.github/workflows/build-images.yml`. It declares top-level `permissions` of `contents: read` and `packages: write` — no more. It has one job running on `ubuntu-latest` with a `strategy.matrix` whose single entry today carries three values: the image name `loop-executor`, the Dockerfile path `docker/loop-executor.Dockerfile`, and the version-file path `docker/loop-executor.VERSION`; a second image must be addable as one more matrix entry and nothing else. The steps in order: `actions/checkout@v4`; a step that reads the matrix entry's version file into a step output; `docker/setup-qemu-action@v3` (this is what makes an arm64 build possible on an amd64 runner, so it is not optional); `docker/setup-buildx-action@v3`; `docker/login-action@v3` against registry `ghcr.io` with username `${{ github.actor }}` and password `${{ secrets.GITHUB_TOKEN }}`; and `docker/build-push-action@v6` with `context: .`, `file` set to the matrix Dockerfile, `platforms: linux/amd64,linux/arm64`, `push: true`, and `tags` set to `ghcr.io/mtgibbs/<image>:<version>` built from the matrix entry and the version output. Do NOT tag `latest`: a mutable tag gives Flux nothing to compare and gives an incident no way to say which commit is running. Add a header comment recording that the GHCR package must be flipped public after the first push, with the exact command `gh api -X PATCH /user/packages/container/loop-executor -f visibility=public`. Create only this file.

T3: update docs/loop-container.md so the manual build is the fallback rather than the procedure. It currently documents a manual `docker buildx build --platform linux/amd64,linux/arm64 --push` as the way the image is produced. That is now what CI does on every push to main that touches the image's inputs. Rewrite that section so the normal path is the workflow — naming it, and naming the version file as the thing to bump to cut a release — and the manual command is kept explicitly as the local-verification fallback for a host with docker, which is still how the acceptance in that document gets run. Do not delete the manual command; it is the only way to verify the image builds before merging a change to it. Change no other file, and do not touch the Dockerfile.
