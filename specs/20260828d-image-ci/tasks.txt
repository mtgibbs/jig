T4: derive the pushed image name from the matrix in .github/workflows/build-images.yml. The `tags` value on the build-and-push step currently hardcodes the image name, so a second matrix entry would publish under the FIRST entry's name — which defeats the only reason the matrix exists. Change that one value so the image name comes from the matrix entry's `image` field rather than being written literally, keeping the registry and owner (`ghcr.io/mtgibbs/`) and keeping the version from the step output exactly as they are. Change nothing else in the file: not the trigger, not the paths filter, not the permissions, not the platforms, not the step order.
