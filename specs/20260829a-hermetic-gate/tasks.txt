T1: make `gate_tmpdir` in `specs/lib/assert.sh` choose a workspace it has proven it can execute from, instead of taking the first one `mktemp -d` hands it. Try candidates in this order — `$TMPDIR`, `$CLAUDE_CODE_TEMP_DIR`, `$HOME/tmp`, `/tmp` — skipping any that is empty, does not exist, or cannot be written to, and for each remaining one create the workspace inside it and run the exec probe the function already builds: write a small script, mark it executable, run it. The FIRST candidate whose probe succeeds wins; set `T` to that workspace and return exactly as the function returns today, so that every gate in the repo behaves identically on a host where the current code already worked. The probe is the only arbiter — never decide by matching a directory name, reading mount flags, or inferring that a path implies a container, because `/home/agent` exists here and means nothing on a laptop or on the arm64 worker and this library must keep working in all three. Remove each failed candidate's workspace before trying the next, so a run that falls through to the fourth candidate does not leave three abandoned directories behind. When NO candidate passes, exit 2 exactly as today — this is fail-closed and it stays fail-closed, because a gate that cannot run must report neither pass nor fail, and a change that turns "cannot run" into "passes" is the whole defect this spec exists to prevent. Extend only the diagnostic: alongside the environment line it already prints, name each candidate tried and how it failed — absent, unwritable, or noexec — because a gate that cannot run should explain itself on the first read rather than the third. Do not neutralise any environment variable in this task; T2 owns that.
T2: neutralise the harness's own outbound configuration in `specs/lib/assert.sh`, so that a fixture is unconfigured unless it says otherwise. `HARNESS_REPORT_URL` and `HARNESS_REPORT_TOKEN` are set in every harness container, and a gate that inherits them is a gate whose "with nothing configured" fixture runs against the production coordinator — which on 2026-08-29 both falsified `T01-push-artifacts` ac1 and posted fixture rows to the live fleet board. UNSET them, do not assign a placeholder: a sentinel value is still a URL, and something will eventually POST to it, which is the same defect wearing a different costume. Do this at the point where sourcing the library establishes the ground, so a gate author gets it without writing a line and cannot forget it in the one gate where it matters. Neutralisation must not take the capability away: a gate that means to exercise the CONFIGURED path must still be able to, per invocation and explicitly, exactly as `runloop_logged "$T/on" HARNESS_REPORT_URL="$COORD_URL"` does today — the default becomes absent, it does not become unavailable, and every existing gate that configures a stub coordinator must keep passing untouched. Change no assertion's text or meaning anywhere: this task changes what a gate inherits, never what it measures.
T3: document the ground a gate stands on, in `specs/README.md` and in the header comment block of `specs/lib/assert.sh`. State the rule as a rule — a gate establishes the conditions it needs and neutralises the ones it does not, in the library rather than in each gate's text — and say why that placement is the point: a per-gate convention is forgotten precisely in the gate written before the container setting that makes it matter. Record both defects as the evidence, briefly and concretely, because the abstract rule is not persuasive and the two measurements are: a `noexec` `/tmp` reduced a correct gate to one line and exit 2, and an inherited `HARNESS_REPORT_URL` left an assertion named "with nothing configured" testing the configured path and posting to production while it did so. Say what a gate author must do to opt back in — configure explicitly, per invocation — and warn about the shape that looks safe and is not: an assertion phrased as "exited non-zero" or "finished under 30s" is SATISFIED by a mock that died of Permission denied, so a gate that inherits a broken workspace can pass green while measuring nothing. Change no executable behaviour: this task adds documentation and comments only.
