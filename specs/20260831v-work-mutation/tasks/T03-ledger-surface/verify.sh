#!/usr/bin/env bash
# Gate for T03-ledger-surface (20260831v-work-mutation). RED BY CONSTRUCTION: this stub fails until it is
# replaced with the task's real acceptance checks. Author the checks FIRST, run them red,
# capture evidence/red-before-green.txt, and only then build the deliverable.
set -uo pipefail
echo "  FAIL  T03-ledger-surface: gate is an unwritten stub — author this task's acceptance checks" >&2
exit 1
