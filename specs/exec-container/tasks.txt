T2: write docker/loop-executor.Dockerfile. It is modelled on the existing coding-harness-qwen image, minus everything a one-shot executor does not need — no tmux, no gh CLI, no persistent HOME. That reference lives in another repo and is NOT readable from this worktree, so everything you need from it is reproduced here; do not go looking for it. Base on `node:22-bookworm-slim`, install `git`, `ripgrep`, `ca-certificates`, `curl` and `tini` with `--no-install-recommends` and clean the apt lists in the same layer, then install opencode with `npm install -g "opencode-ai@${OPENCODE_VERSION}"` where OPENCODE_VERSION is an `ARG` pinned to 1.17.10. The image MUST build unchanged for linux/amd64 and linux/arm64: do NOT write a `--platform` flag, do NOT use an architecture-qualified base tag, and if any step downloads an architecture-specific artifact resolve the architecture with `dpkg --print-architecture` at build time rather than writing a literal. Give it an ENTRYPOINT through tini that takes the prompt as its argument and runs the executor against `$ROOT`. Do not add a USER line pinning a uid: the binding passes `--user` at run time (spec.md outcome 3), and a baked-in USER would fight it.

T3: write scripts/loops/build-container.env, the strategy that selects this binding, in the same shape as scripts/loops/build-codex.env. It sets STRATEGY_DESC and STRATEGY_PHASES exactly as build-converge does — one `build` phase, nothing after it — and additionally points RALPH_EXEC_CMD at scripts/exec-container.sh so `run-loop.sh build-container specs/<feature>` runs the containerised executor. Read build-codex.env first and match it; adding an executor is meant to cost one binding plus one strategy file and nothing else. Change no other file.

T4: write docs/loop-container.md, and this task is LAST because it documents what the others built. Two sections. First, the multi-arch build and push: the exact `docker buildx build --platform linux/amd64,linux/arm64` invocation against docker/loop-executor.Dockerfile, including why the platforms are passed to buildx rather than written into the Dockerfile. Second, the ACCEPTANCE that no gate in this repo can run, stated as a runbook with an owner: (a) the buildx build succeeds for both platforms, and (b) the fleet-dispatch verification sketch for item 2 — one spec run via exec-qwen.sh and one via exec-container.sh, with `.evidence/` differing ONLY in the `binding` field. Say plainly that these require a host with docker, that the harness container has none, and that this is why they are a runbook rather than acceptance criteria in verify.sh. Reference the environment the container needs (spec.md section 3.2) so a reader can tell why a run fails when the LiteLLM key is absent.
