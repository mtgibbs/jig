T3: write scripts/loops/build-container.env, the strategy that selects this binding. IMPORTANT: the executor auto-rejects reads of `*.env` files as a secrets guard, so you cannot read the sibling strategy files and must not try — everything you need is here. The file sets exactly three things, in this shape: a `STRATEGY_DESC="..."` one-line description (run-loop.sh --list prints it), `STRATEGY_PHASES="build"` — one build phase and nothing after it, identical to build-converge — and `export RALPH_EXEC_CMD="${RALPH_EXEC_CMD:-${SCRIPT_DIR:-scripts}/exec-container.sh}"`, keeping that exact parameter-expansion shape so an operator override still wins. Do NOT export RALPH_AGENT. build-codex.env does, because Codex is a different executor family that would otherwise be filed as qwen — but this binding runs the SAME qwen, only reached through a container, and item 2's acceptance is that `.evidence/` differs ONLY in `binding`. Setting RALPH_AGENT here would make `agent` differ too and quietly break the one comparison this whole spec exists to enable. Change no other file.

T4: write docs/loop-container.md, and this task is LAST because it documents what the others built. Two sections. First, the multi-arch build and push: the exact `docker buildx build --platform linux/amd64,linux/arm64` invocation against docker/loop-executor.Dockerfile, including why the platforms are passed to buildx rather than written into the Dockerfile. Second, the ACCEPTANCE that no gate in this repo can run, stated as a runbook with an owner: (a) the buildx build succeeds for both platforms, and (b) the fleet-dispatch verification sketch for item 2 — one spec run via exec-qwen.sh and one via exec-container.sh, with `.evidence/` differing ONLY in the `binding` field. Say plainly that these require a host with docker, that the harness container has none, and that this is why they are a runbook rather than acceptance criteria in verify.sh. Reference the environment the container needs (spec.md section 3.2) so a reader can tell why a run fails when the LiteLLM key is absent.
