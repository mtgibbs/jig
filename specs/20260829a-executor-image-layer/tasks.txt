T1: split the image into docker/harness-base.Dockerfile and a thin docker/loop-executor.Dockerfile, and wire both into the CI matrix. The base carries the LOOP and no model CLI: copy scripts/ and specs/lib/ into HARNESS_HOME=/harness, put $HARNESS_HOME/scripts on PATH, keep the apt set the current file already installs (git, ripgrep, ca-certificates, curl, tini) since those move DOWN into the base rather than being re-solved, and set ENTRYPOINT to run-loop.sh under tini exactly as the current file wraps opencode under tini. Do not install opencode, codex or claude in the base — the base having no CLI is the whole point of the split, and it is what lets a third party write FROM harness-base and add their own. The derived docker/loop-executor.Dockerfile becomes FROM the base plus the single npm install -g opencode-ai line and nothing else: no apt-get, no second copy of the harness, no re-declared PATH. Add docker/harness-base.VERSION starting at 0.1.0 alongside the existing docker/loop-executor.VERSION, and add the base to .github/workflows/build-images.yml for linux/amd64,linux/arm64 the way the two existing entries are declared. The base must be PUSHED BEFORE the derived image is built — a matrix runs its entries in parallel, so a plain third matrix row would race and the derived build would pull a base that does not exist yet; see OQ2 for the two orderings under consideration and pick one, but do not leave them parallel. Say in a comment in each Dockerfile why the base has no CLI and why the derived file is thin, because the next person to add an executor reads these two files and nothing else. Change no script, no strategy file and no dispatcher code in this task.
T2: give run-loop.sh a strategy SEARCH PATH so a repo that is not this one can bring its own strategy and its own binding, in scripts/run-loop.sh. Resolve <name>.conf from $HARNESS_REPO_ROOT/.harness/loops/ first and $SCRIPT_DIR/loops/ second, first match wins, so a consumer repo can both add strategies and shadow a built-in one. Export HARNESS_REPO_ROOT — the git toplevel of the repo being worked on — BEFORE the line that sources the conf: the conf cannot use $ROOT because ralph-build.sh is what exports ROOT and it has not run yet at that point, which is precisely the bug a consumer conf would hit first and the reason this ordering is an acceptance criterion rather than an implementation detail. Fallback-first, in the shape ralph-build.sh's per-task gate resolver already established: a repo with no .harness/ directory is not misconfigured, it is every repo that exists today, and it must resolve the built-in conf and run exactly as it does now. Extend --list to show strategies from both locations with which is which, and to NOT list a built-in that a consumer conf shadows, since listing both names would tell a reader the opposite of what will run. When a strategy is found in neither location, exit non-zero naming BOTH paths that were searched rather than the old single-path message. Do not change the conf contract itself, do not add STRATEGY_TOOLS here — T3 owns it — and do not touch any exec-*.sh binding.
T3: let a strategy declare the executable it needs, and preflight it, in scripts/run-loop.sh and scripts/loops/README.md. A conf may set STRATEGY_TOOLS to a space-separated list of executables; each is checked with command -v and an absent one stops the run before any work starts. This is the check that turns "strategy build-codex in an image with no codex binary" from a confusing mid-run shell error into a preflight failure that says what is missing. Join the EXISTING accumulation rather than adding a second preflight block: run-loop.sh already collects missing spec-declared Tools into one list and exits 3 with the message that the container needs attention rather than another retry, and a second block would produce a second message and a different exit code for the same class of fault. A missing spec tool and a missing strategy tool together must produce ONE message listing both and ONE exit 3. The message must name the missing executable AND the strategy that declared it, because "codex not found" without the strategy name sends the reader to the spec header, which is the wrong file. Fail closed: never a warning, never a continue. A conf that sets no STRATEGY_TOOLS behaves exactly as today. Document the key in scripts/loops/README.md's contract section as a declaration — it fits the existing "may only declare and export" rule and does not widen it — and note there the known duplication that OQ4 records. Do not add STRATEGY_TOOLS to any existing conf in this task; the strategies that need it get it when their images exist.
T4: let dispatch ask for a strategy other than the default and land it on an image that can run it, in scripts/dispatch/dispatcher.py. Widen parse_intent's grammar by exactly one optional trailing field: four whitespace-separated parts still yields build-converge unchanged, five yields the fifth as the strategy, and six or more is still a parse failure returning None. The HTTP path needs NO change — api.py already reads body.get("strategy", "build-converge") and passes it through, so the text path is the only one pinned and the only one this task touches. Then resolve the worker IMAGE from the requested strategy: HARNESS_WORKER_IMAGE_<STRATEGY_UPPER_SNAKE> when set, falling back to today's single HARNESS_WORKER_IMAGE, which is a map and nothing more — no policy, no fallback chain, no judgement, because the dispatcher staying describable in a paragraph is a named cliff in docs/design/fleet-dispatch.md and this is exactly the kind of code that erodes it. The strategy named in the rendered Job's STRATEGY env and the strategy used to resolve the image must be the SAME requested strategy: a run launched with one strategy's name on the env and another strategy's image is the failure this task exists to make impossible, and it is the mutant the gate must catch. Change nothing else about render_job — not the env list, not the nodeSelector, not the pod spec — the Job body is the deploy-side follow-on and is out of scope.
T5: write docs/executors.md and finish the loops/README.md contract update. The doc's job is to let someone who has never read this repo add Claude, Gemini or their own agent without a PR here. Carry the three-layer table from the spec's Entities section — the loop never varies, the strategy varies per run, the binding and its CLI are the extension point — and give a copy-pasteable Dockerfile that is FROM harness-base plus a CLI install plus a binding plus a conf, since a worked example is what people actually copy and an untested one is a bug handed to the reader. State plainly that the harness ships NO credentials and that auth belongs to the derived image and the operator, so nobody goes looking for a key the base was supposed to provide. Contrast exec-container.sh with an in-pod direct binding explicitly and in the same section: exec-container.sh runs the loop on the HOST and sends each prompt into a fresh container, while in a k8s Job the loop is already inside the pod and uses a direct binding over the network — two things called "container" one line apart is the constitution's similar-but-different trap in its purest form, and a reader who conflates them writes docker-in-docker. Record what this does NOT provide: no Job body, no clone step, no credential provisioning, and note that sourcing a conf from the worked repo executes shell from that repo while explaining why that is not a new hole — the loop already runs that repo's verify.sh and gives an agent write access to its tree. Change no executable behaviour in this task.
