T1: put the environment reset in specs/lib/assert.sh and run it on load, so a gate gets it by sourcing the file every gate already sources and no gate carries a line of its own. Unset exactly five variables — HARNESS_REPORT_URL, HARNESS_REPORT_TOKEN, RALPH_FORCE_ALL, RALPH_FORCE_FROM, RALPH_SATISFIED_TIMEOUT — and write the incident behind each one beside it, in the file, not only in the spec: the first two because a fixture loop that inherits them POSTS TO THE PRODUCTION COORDINATOR and put eight rows keyed `spec=fx` on the live fleet board where they share harness#46's oldest-first eviction and can push out the record of a real run; RALPH_FORCE_ALL and _FROM because a gate that inherits them cannot skip anything inside its own fixtures, which made four of six assertions in 20260829b unable to pass whatever the executor wrote and one pass for the wrong reason; RALPH_SATISFIED_TIMEOUT because a gate that sets it per case is silently overridden by an ambient value. UNSET, never a sentinel — a sentinel URL is still a URL and something eventually POSTs to it. The reset runs at source time, which is what makes it unforgettable and also what makes it bounded: anything a gate sets AFTER sourcing is the gate's own choice and must survive, because several gates configure these variables deliberately for the case they are testing. assert.sh is sourced and never executed, and its own header says it must not exit at the top level and must not run anything on load — an unset neither exits nor prints, and it must stay that way, because a reset that produces output breaks every assertion in other specs that compares a gate's stdout. Then put the same reset at the INVOCATION BOUNDARY too, in scripts/ralph-build.sh's run_gates, because assert.sh alone reaches only the 31 per-task gates that source it and there are 24 monolithic gates that source nothing — 20260801a through 20260828j predate the per-task layout and define ok/no/pend inline, and several of them drive the real loop, which makes them exactly the gates that can post to a coordinator. Clearing the set in run_gates before it invokes a gate covers every gate the loop runs whatever that gate sources, while assert.sh covers a gate run by hand or by gate-selftest; the two placements cover different populations and neither is redundant. Do not instead add a source line to all 24: that is 24 edits which must not change one verdict, and it reinstates the rule-you-must-remember for gate number 25. Do not touch gate_tmpdir in this task and do not delete either local copy; T2 and T3 own those.
T2: delete the three local copies and prove the central reset covers what they covered, in specs/20260828o-evidence-egress/lib/fixtures.sh, specs/20260829a-executor-image-layer/lib/fixtures.sh and specs/20260829b-resume-bound/lib/fixtures.sh. 20260829a uses a bare `unset` at source time; 20260829b defines an `_fx_env()` helper wrapping `env -u` around each loop invocation; 20260828o defines a DIFFERENT `_fx_env()` with the same name that covers only two of the five variables. Two helpers sharing one name and covering different sets is what a rule with an adoption problem looks like, and it is why deleting them is the point rather than the tidying. Removing them is not tidying: while they exist the central reset is UNPROVEN, because the two gates that most need it would pass on their own patches and tell you nothing about the file every other gate loads. Deleting them makes those gates the migration's own test. The bar is not "those gates pass" but "those gates return what they returned" — record each spec's exit codes with the local copies in place, remove them, and compare; a spec that was already red must stay red for the same reason, because a spec whose verdicts MOVE is a spec that broke something while claiming to make it hermetic. Keep every other line of both fixture files, including the comments that explain their sleep caching and their fixture shapes; only the environment handling goes. Both files carry a comment deferring this work to `20260829a-hermetic-gate`, a spec id that collides with executor-image-layer and never existed — those comments go with the code they explain, and the third citation in docs/runs/2026-08-29-the-run-that-lied.md is corrected to this spec's id. Add no new behaviour in this task.
T3: give gate_tmpdir a location and a voice, in specs/lib/assert.sh. The containers mount /tmp as a noexec tmpfs, deliberately, and the compose file asks for it — so gate_tmpdir builds its probe, discovers it cannot execute, and gives up printing `environment: <hostname>`, which is a fact about the machine and not an instruction. Every gate in the repo is one container setting away from unrunnable and the operator has to already know a variable that appears in no gate's text. Honour GATE_TMPDIR: when it is set, create the workspace under it, so an operator can point gates at a mount that executes without touching a container's hardening. When the probe fails, say which variable to set and show a path that would work, instead of naming the host. Keep exit 2 exactly as it is — a workspace fault is neither a pass nor a failure, and a gate that turns it into a FAIL files an environment problem in the run's evidence as a defect in the work. Keep exporting PYTHONDONTWRITEBYTECODE and PYTHONPYCACHEPREFIX. Do not try to mount anything and do not relax the container's noexec: this task makes a gate SURVIVE that setting and say so, it does not argue with it. Note plainly, in the file, that the failure message is asserted by this spec's gate while a real noexec run is not — a portable gate cannot mount a filesystem, and a check that claims to prove more than it does is the thing this spec exists to remove.
T4: make the protection unforgettable and prove it stays that way, in this spec's own gate and in specs/lib/assert.sh's header. A brand-new gate that sources assert.sh and calls nothing else must already be hermetic — that is the whole design, and it is the assertion that distinguishes this from the two local patches that came before it. Assert that no file under specs/ re-implements the quarantined set locally, scoped to executable lines with comments stripped, because the variable names appear in this spec's own prose, in the write-up, and in every comment explaining why they are quarantined — a tree-wide grep would report a finished migration as unfinished forever, and it must also exclude the gate doing the checking, whose own source contains the literal strings it searches for. Assert that the set appears in exactly ONE place and that each entry carries the incident it came from, so the next person to add a variable is told what kind of evidence earns a place on the list. Say in assert.sh's header that sourcing it is the protection, so a reader who opens the file to add a helper learns the property before they can break it by moving the reset into a function nothing calls.
