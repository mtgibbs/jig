T1: implement the task-scope guard in scripts/ralph-build.sh per spec §6 — opt-in tasks/T<NN>-<slug>/scope file (one git pathspec glob per line), globs injected into the executor prompt, any out-of-scope change fails the attempt before the gate with targeted feedback and a full tree reset (never filter-the-commit), bookkeeping writes excluded, empty scope refused up front with exit 3, no scope file means behavior unchanged; document the scope file in specs/TEMPLATE.md §11
