T1: write verify.sh — mktemp fixture corpus (killed + survivor + wrong-aimed mutants) proving the emission tells verdicts apart; schema and marker/prose-agreement checks via jq; install-time diff assertions; hermetic-default (no SELFTEST_EVID => byte-identical fixture) with the flagged run as positive control; generator render + survivor-first + planted-bogus link-check control; run RED and record
T2: add the SELFTEST_EVID seam to scripts/gate-selftest.sh — slug/run-id derivation from the task path, install-time mutant-vs-target diff capture (metadata stripped, /dev/null for absent targets), emit_row at each verdict decision with instead-ids on WRONG-REASON and full WHY re-read from the mutant file, run_complete marker before the summary exit; unset flag writes nothing
T3: write scripts/mutant-ledger.py — load selftest-*.jsonl taking the latest run_complete run per corpus, render mutant-ledger.md (relative links, collapsible ````diff blocks) and mutant-ledger.html (verdict tiles, filter, survivor-first cards, colored diffs, drift badges) deterministically
T4: run the recorded sweep over all six committed corpora with SELFTEST_EVID=.evidence, generate and commit the ledger, re-run specs/20260828k-gate-selftest/verify.sh green, add the store section to .evidence/README.md
