T1: write scripts/selftest-sweep.sh per spec §6 — resolve _SD and R from $0, enumerate corpora with find specs -type d -name mutants -path '*/tasks/*' | sort, support --dry-run printing one 'would run: specs/<slug>/tasks/<task>' line per corpus and running nothing, otherwise per corpus print '== specs/<slug>/tasks/<task>' and run SELFTEST_EVID="$R/.evidence" bash "$_SD/gate-selftest.sh" "<corpus dir>" tracking the worst exit code without stopping on red, then always run python3 "$_SD/mutant-ledger.py" --evid "$R/.evidence" --out "$R/.evidence", exit 0 only if every corpus and the ledger step succeeded; chmod +x the file
T2: append the exact bullet from spec §6 to .evidence/README.md's "What lives where" list, directly after the mutant-ledger.{md,html} bullet
