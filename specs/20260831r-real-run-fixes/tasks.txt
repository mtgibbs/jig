T1: PIN — the loop already runs the spec-level convergence gate on per-task specs, and this gate holds it. Filed as finding H5 after jig run 1 (2026-08-31) on a misread of run_gates (scripts/ralph-build.sh ~331: $VERIFY appears only in the monolithic fallback); the diagnosis was WRONG — the post-task-loop convergence block (~661-679) runs every task gate under STRICT and then the spec-level verify.sh for per-task specs, silently when green, STOP exit 2 when red. Run 1 DID run STRICT v1; the gate authored to catch the "bug" passed red-first and became the proof. Keep the behavior pinned exactly as observed: convergence runs at the end of a per-task run (once), a red convergence gate stops the run nonzero, a green one converges at 0, and the legacy no-tasks-dir path is still judged by the spec verify. If this gate goes red, the convergence block regressed — do not weaken the gate.
T2: run-loop.sh refuses early when a strategy's required environment is missing. Jig run 1 aborted MID-RUN with `Authentication Error, No api key passed in` because exec-opencode.sh takes credentials from the environment by contract (20260825c) and nothing checked the environment up front. Add a declarative knob: a strategy conf may set STRATEGY_ENV_REQUIRED="VAR1 VAR2"; after sourcing $ENV_FILE, run-loop.sh iterates the names (bash-3.2-safe `eval`/`${!v}` indirection, values NEVER printed) and refuses with exit 3 naming each unset-or-empty variable BEFORE any phase runs, alongside the existing tools/MCP preflight. Declare STRATEGY_ENV_REQUIRED="OPENCODE_QWEN_KEY" in scripts/loops/build-then-judge.conf (its default executor binding reads that variable). A strategy with no STRATEGY_ENV_REQUIRED behaves exactly as today.
T3: the judge logs its prompt instead of erroring every round. scripts/ralph-judge.sh:258 calls log_prompt but never sources scripts/ralph-log.sh (which defines it), so every judge round prints `log_prompt: command not found` to stderr and no judge prompt is ever recorded — observed in both jig runs on 2026-08-31. Source ralph-log.sh in ralph-judge.sh the same guarded way ralph-build.sh does (~line 378: `if [ -f "$(dirname "$0")/ralph-log.sh" ]`), and make the log_prompt call safe when the file is absent (`command -v log_prompt` guard or a no-op fallback), matching the repo's cross-file-call house rule. Do not change what the prompt says or when it is built.
T4: the index-row guard compares the label the indexer actually writes. scripts/ralph-build.sh lines ~613-616 probe the run index with `${HB_TASK%% *}` — the first word of the tasks.txt line, colon included ("T1:") — while loop-index.py writes `"task": "T1"` (bare label). Every per-task run therefore logs `WARN: ... has no row for T1: — indexing ran but did not record this task` even when the row exists (jig run 1, 2026-08-31: the row was present; the warning lied). Change both uses in that guard block to `${HB_TASK%%:*}`, the same expansion the loop-metrics.sh call four lines up already uses. Touch nothing else — the guard's warn-never-fail contract stays.
T5: the loop refuses an attempt that edits the spec dir — the executor does not get to change the ruler. In jig run 1 the executor edited its own task gate (specs/*/tasks/T01-*/verify.sh) in the same attempt that satisfied it; the edit happened to widen coverage, but a gate edit that WEAKENS the check would have ridden into the commit the same silent way — reward hacking's front door. Ratified 2026-08-31: fail-closed. In scripts/ralph-build.sh, immediately after the scope guard and BEFORE the gate runs, detect any change (tracked diff or untracked file) under "$SPEC_DIR" made by the attempt; if present, reject the attempt wholesale exactly like the scope guard does (name the files in the refusal and the feedback, log_failure before the reset, _reset_tree, continue), with feedback telling the executor: the spec and its gates are the operator's — report a gate gap in the transcript instead of editing it. A gate blind spot then deadlocks to escalation, which is correct — a human fixes the gate. Attempts that touch only non-spec paths are unaffected.
