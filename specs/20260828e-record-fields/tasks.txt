T4: teach specs/20260826a-evidence-replayable/verify.sh to assert VALUES, not just keys. Its AC-7 loop checks each of the seventeen schema fields with `jq -e 'has("<field>")'`, and `has()` is true for an empty string — which is precisely how three empty fields shipped behind a green gate. Keep every existing presence check exactly as it is, and add two value assertions after them: that `outcome` is one of the four declared literals `passed`, `failed`, `noop`, `stillborn`, and that `duration_s` is a non-negative integer. Both must be written so they FAIL on the values that ship today — an empty-string outcome and a null duration — because a check that passes on the current broken record would be no check at all. Preserve the null-vs-0 discipline that file already documents: `verify_rc` and `bytes_patch` are legitimately null in cases the schema names, so do not assert those two are non-null. Touch no other file and no other spec.


T5: stop the task-level stops from overwriting an attempt's own record, in scripts/ralph-build.sh. Each attempt ends exactly once — stillborn, noop, passed, or gate-rejected — and the record for that attempt is written at the point it ends. Two of the five log_meta call sites are not attempt endings at all: the stop after every attempt is exhausted, and the final stop where the STRICT gate finds unbuilt work. Both are TASK-level events, both reuse the last attempt's number, and both therefore rewrite a record that the attempt already wrote correctly — a no-op attempt records `noop` and is then immediately overwritten with `failed` by the exhausted-attempts stop. Make those two sites leave an existing record alone: they may only write when the attempt has not already recorded itself. Do not delete either call — an attempt whose gate rejected it has no other site that records it, so removing them would lose that case entirely. Do not change the three attempt-ending sites, and do not change what either stop prints or the exit code it uses.
