T1: stamp the attempt's timing in scripts/ralph-build.sh. Set `LOG_STARTED` to the current unix seconds once per attempt, at the top of the attempt loop beside the existing `HB_ATTEMPT="$attempt"` assignment, so it marks when the attempt began rather than when the task did. Then set `LOG_ENDED` to the current unix seconds immediately before each existing `log_meta` call — there are four of them and they are the only places an attempt's record is written. Use `date +%s`, matching how the rest of this file reads the clock, and guard nothing: an unset clock is not a failure mode worth code. Do NOT change `log_meta` itself or anything in `scripts/ralph-log.sh`: the writer already reads these two variables and already derives `duration_s` from the pair, emitting null when `LOG_ENDED` is unset, and that logic is correct. Add no new `log_meta` call in this task; T2 owns the outcomes and T3 owns the missing call site.

T2: set the outcome at each place an attempt ends, in scripts/ralph-build.sh. Immediately before each of the four existing `log_meta` calls, set `LOG_OUTCOME` to exactly one of the four literals the record's schema declares — `passed`, `failed`, `noop`, `stillborn` — choosing by what actually happened at that point: the executor-did-not-start abort (the branch that prints "the executor did not start" and exits 3) is `stillborn`; the branch that prints "passed verify" is `passed`; the stop after all attempts are exhausted without the gate going green is `failed`; and the final stop where every task passed but the STRICT gate found unbuilt work is `failed`. Use the literals exactly as written here, lowercase, no other spellings — a value outside the declared set is required to read as a failure by the consumer, so an invented word is worse than an empty one. Change nothing else.

T3: give the no-op branch a record, in scripts/ralph-build.sh. The branch that prints "attempt N changed nothing — a no-op is a failure, not a pass" currently calls `hb_write` and then `continue`s, so a no-op attempt leaves NO record at all and the `noop` outcome in the schema is unreachable. Add the same pair of statements the other four sites will have — set `LOG_OUTCOME` to `noop`, stamp `LOG_ENDED`, then call `log_meta` with the task label and attempt number exactly as the other call sites do — placing them before the `continue` so they actually run. Do not change what that branch decides or the feedback it composes; a no-op remains a failed attempt that retries. This is the fifth and last `log_meta` call site.

T4: teach specs/20260826a-evidence-replayable/verify.sh to assert VALUES, not just keys. Its AC-7 loop checks each of the seventeen schema fields with `jq -e 'has("<field>")'`, and `has()` is true for an empty string — which is precisely how three empty fields shipped behind a green gate. Keep every existing presence check exactly as it is, and add two value assertions after them: that `outcome` is one of the four declared literals `passed`, `failed`, `noop`, `stillborn`, and that `duration_s` is a non-negative integer. Both must be written so they FAIL on the values that ship today — an empty-string outcome and a null duration — because a check that passes on the current broken record would be no check at all. Preserve the null-vs-0 discipline that file already documents: `verify_rc` and `bytes_patch` are legitimately null in cases the schema names, so do not assert those two are non-null. Touch no other file and no other spec.

T5: stop the task-level stops from overwriting an attempt's own record, in scripts/ralph-build.sh. Each attempt ends exactly once and records itself where it ends. The stop after every attempt is exhausted, and the final stop where the STRICT gate finds unbuilt work, are TASK-level events that reuse the last attempt's number — so they must only write when the attempt has not already recorded itself. Guard on a per-attempt "already recorded" flag reset at the top of each attempt, NOT on the end timestamp: that timestamp is stamped on every attempt, so a guard reading it is always false and the site silently never fires, losing the only record a gate-rejected attempt would get. Do not delete either call.

