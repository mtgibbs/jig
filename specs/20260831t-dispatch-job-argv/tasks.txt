T1: render_job() carries the intent as argv, and the render→parse seam is gated. Issue #117 (found live, pi-cluster #219 activation, 2026-08-31): the rendered Job set REPO/SPEC/STRATEGY env — variables nothing in the worker reads — and no args, so entrypoint.sh's `exec run-task.sh "$@"` expanded to nothing and every dispatched run died at the parser (exit 64, `<spec-dir> is required`) before cloning. Replace the container's env block with `args: [<spec>, --repo, <repo>, --strategy, <strategy>]` — exactly run-task.sh's ONE documented calling convention — and render NEITHER the dead env names NOR a `command` key (a command would displace the ENTRYPOINT that writes the clone credential). Gate the SEAM, not just the shape: feed the rendered argv to the real scripts/run-task.sh with HARNESS_DIR pointed at a nonexistent path and assert exit 66 (past the parser, refused later, no network), with an empty-argv control still exiting 64 so the assertion can't go tautological; pin that entrypoint.sh still passes "$@" through. Amend the two historical gates that pinned the env shape — 20260828f ac4 and 20260830a T01 (ac3 completeness, strategy_of, ac6 exact-shape) — to pin the argv truth with dated comments naming this spec, and prove they can still fail by running them red against the pre-fix renderer (evidence/red-before-green.txt). Update scripts/dispatch/README.md's Bound Job list. Worker image, entrypoint.sh and run-task.sh are untouched — this is a dispatcher-image-only fix.
