T1: teach harness_roots the deeper level, in scripts/loop-index.py. It currently reports the flat layout (runs directly under `base`) and the scoped layout (runs one level down, under a directory whose name becomes the scope). A third layout now exists: runs TWO levels down, under `<slug>/<host>/`. Extend the existing loop so that when a scope directory does not itself hold runs, its immediate subdirectories are examined, and any that DO hold runs are appended as `(that directory's path, the OUTER directory's name)` — the scope stays the outer name, which is the spec slug a reader recognises, and is never the host. Apply the same `REPO_DIR_RE` and the same "this is a run dir, not a scope" skip to the deeper level that the existing level already applies, so a run directory is never mistaken for a scope. Do NOT change `glob_runs`, and do not add a second traversal elsewhere: `load_status` calls `harness_roots` with `"*.json"` and the log walk calls it with `RUN_GLOBS`, so this one function is the only place both stores can be fixed together, and a fix made anywhere else leaves the status store blind. The flat and scoped layouts must keep working byte-identically — both exist in real corpora on disk, and a change that sees the new layout by ceasing to see an old one has moved the blindness rather than cured it. Change no other file.

T2: correct the depth assumption in specs/20260825b-evidence-spec-nesting/verify.sh. It locates artefacts with `find ... -mindepth 2 -maxdepth 2` and then asserts the spec slug is the run directory's immediate PARENT. Runs now sit one level deeper, so it finds nothing and reports "no run directory was created at all" — true about what it looked at, misleading about the tree. Change the search to `-mindepth 2 -maxdepth 3` and assert instead that the slug is the FIRST path element beneath the store root, so both the scoped and the nested layouts satisfy it. Keep `-mindepth 2` exactly as it is: the case that gate exists to catch is a run directory sitting flat in the root with no spec level at all, and `-mindepth 2` is what excludes it — widening the lower bound would delete the check. Apply the same correction to the status assertion immediately below it, which has the identical shape. Touch nothing else in that file and no other spec.

T3: make the reap and the empty-directory pruning handle BOTH layouts, in scripts/ralph-log.sh and scripts/ralph-status.sh. Two separate defects, both from the tree getting one level deeper.

First, the reap in ralph-log.sh was changed from depth 2 to depth 3, which MOVED it rather than widening it: a run in the older `<slug>/<agent>-<pid>` layout now sits at a depth the reap no longer visits and is never collected, and those corpora exist on disk. It must collect a run at EITHER depth. Do not identify a run by its name: `<agent>-<pid>` is indistinguishable from a Kubernetes pod name like `harness-run-7`, which is why the host level is excluded by position everywhere else. Identify it by STRUCTURE instead — a run directory contains files and no subdirectories, while a host directory contains run directories. So at the shallower depth collect only a directory that has no subdirectory of its own, and at the deeper depth collect as it does today. A fresh run beneath an aged host directory must survive, because a directory's mtime tracks its newest child and deleting the host wholesale would take live runs with it.

Second, the empty-directory prune. ralph-log.sh prunes only INSIDE the current run's own slug directory, so a wholly-expired OTHER spec keeps an empty host directory, which keeps its slug directory non-empty, which means the slug directory survives as an empty husk; ralph-status.sh prunes only at depth 1 below its root and has the same defect for the same reason. In each file replace the single prune with two passes rooted at the STORE ROOT rather than at one slug: first remove empty directories at depth 2, then at depth 1. The order is load-bearing — the outer pass can only see a slug directory as empty once the inner pass has removed the empty host directory inside it.

Everything here stays best-effort, keeping the existing `2>/dev/null || true`: this is cleanup, and outcome 8 of specs/20260826a-evidence-replayable says nothing here may fail a loop. Change nothing else in either file.
