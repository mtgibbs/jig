T1: add reap_runs(registry_path, namespace) to scripts/dispatch/dispatcher.py per spec §2 — fold the registry last-wins, query only launched records' Jobs via HARNESS_KUBECTL, append superseding terminal records (job absent/failed -> failed, succeeded -> succeeded, active/indeterminate/kubectl-error -> untouched), make get_run last-match, and expose POST /reap in scripts/dispatch/api.py behind the existing auth
