T1: make the final task's gate strict, in scripts/ralph-build.sh. At the per-task gate call (ralph-build.sh:192, `if out="$(cd "$ROOT" && bash "$VERIFY" 2>&1)"; then`), compute whether this is the last task and pass the answer to the gate as `STRICT`. The last task is `HB_TIDX` equal to `HB_TOTAL`; HB_TIDX is incremented at ralph-build.sh:132 and HB_TOTAL is set in ralph-status.sh:84. If HB_TOTAL is empty, unset or 0 the mode MUST be lenient — a false "last task" would fail every earlier task and stop the run, so an undeterminable answer resolves to the safe mode, not the strict one. The mode must ride as an assignment PREFIX on the call that is already there — `STRICT="$x" bash "$VERIFY"` — and you must NOT add a second call site or a branch containing its own `bash "$VERIFY"`: specs/20260826a-evidence-replayable AC-4 strips comments and counts that exact literal, requires exactly 2, and a third invocation would double every gate's side effects. Do not touch the final STRICT check at ralph-build.sh:275, do not touch the feedback block, and do not change any verify.sh.

T2: say which mode the gate ran in, in scripts/ralph-build.sh. On the line that already reports a task passing verify (`echo "  ✓ $task passed verify (attempt $attempt)"`), and on the failing counterpart, make the run log state whether that task's gate was the strict one or the lenient one. Today a reader cannot tell from the log which gate produced a verdict, and this spec makes that difference load-bearing. Keep it to the existing echo lines — do not add new log lines, do not touch bus_say, and do not change the gate call itself, which T1 owns.

T3: document the rule where gate authors will read it, in specs/TEMPLATE.md. The template already explains the three verdicts and what `pend` means. Add that `pend` is bounded by task POSITION: on the final task there is no later task to defer to, so the loop runs that task's gate with STRICT=1 and a `pend` there is a failure. State plainly that this is why a whole-run `STRICT=1` is the wrong fix — it promotes every later task's pend from the first task onward and no early task can pass. Do not touch any verify.sh and do not restate the rule in a second place; one home for it.
