T1: write scripts/spec-field.sh, the only thing in the harness that knows the spec-header grammar. `scripts/spec-field.sh <spec.md> <Key>` prints one value per line for `- **<Key>:** a, b, c`, stripping surrounding whitespace and dropping empty entries, and exits 0; a key that is ABSENT exits 1 and prints nothing, because "declares nothing" and "declares none" are different statements and the preflight must tell them apart. `<spec.md> --list` prints every declared key one per line. A missing argument or an unreadable file exits 2 with a usage line on stderr — a parse failure must never degrade to "declares nothing". Parse ONLY the region above the FIRST line that is exactly `---`, and skip any line inside a ``` fenced block: this spec's own spec.md carries example declarations in a fence in section 3.1, so a parser that reads the whole file will report `jq` and `swift` as required and fail its own gate. bash 3.2 only — no mapfile, no associative arrays. Quote every heredoc delimiter. Nothing calls this yet.

T2: document the grammar in specs/TEMPLATE.md and specs/README.md. In TEMPLATE.md add `Tools:`, `MCP:` and `Permissions:` to the header block with a comment naming what each is for, and add a short subsection carrying the table from spec.md section 3.2 — which fields are ENFORCED (Tools, MCP: fatal at preflight) and which are RECORDED ONLY (Permissions: printed, never verified). State plainly that Permissions is unenforced until the container and egress mechanism exists; a field that looks checked and is not is the shape of a gate that cannot fail, and the amendment "Gates must prove they can fail" is the reason we say so out loud rather than shipping it quietly. Also document that an ABSENT key and an explicit `none` are different. In specs/README.md add the three keys to the layers table so a consumer repo sees them where it already looks. Do not add a declaration to any EXISTING spec — absence is the correct default and twelve mechanical edits are not this change.

T3: add the preflight to scripts/run-loop.sh, and this task is LAST because it edits the script the strategy is executing. Extend the existing fatal preflight block (run-loop.sh:32-38, the one that already rejects an unknown strategy, a spec without spec.md+verify.sh, and the main branch) — do NOT add a second preflight elsewhere and do not touch ralph-build.sh or ralph-judge.sh, so a hand-run loop is unaffected. For every entry of `Tools:` that is not `none`, test `command -v`; collect EVERY miss and report them all in one run, each naming the tool and the spec file that declared it, then exit 3 — the same code ralph-build.sh's stillborn guard uses for "the container needs attention, not another retry", which is exactly what a missing binary is. For a non-`none` `MCP:` value, require the executor's config file to be present in the worktree and exit 3 naming it if absent; DERIVE that path from RALPH_EXEC_CMD — the literal string `opencode.json` must not appear in run-loop.sh, because a codex or container binding would fail a check that cannot apply to it. Print `Permissions:` in the run banner under a label saying they are recorded and NOT verified. The preflight must create, modify and delete nothing. A spec declaring no keys at all must behave byte-identically to 8fa87e3. Bash reads a running script by byte offset, so expect a nonzero exit after this task; check `bash -n` before treating it as a defect and re-invoke the loop rather than resuming it.
