{"agent":"qwen","pid":129752,"repo":"run-evidence","branch":"run/evidence-replayable","spec":"specs/evidence-replayable","task":"T1: add the prompt writer to scripts/ralph-log.sh. Define `log_prompt <task-label> <attempt> <text>` in the same shape as log_failure (ralph-log.sh:125): return 0 immediately unless LOG_OK=1, resolve the destination with `log_path \"$1\" \"$2\" prompt.md` — never string-build a path — and write `$3` to it byte-for-byte with `printf '%s' \"$3\"`, NOT echo (a prompt beginning with `-n` or containing a backslash must survive intact). Wrap the write as `{ ...; } > \"$f\" 2>/dev/null || true` so a read-only or full LOG_DIR cannot fail the loop, and on failure print one line to stderr naming the artifact — a silent helper is one nobody notices is broken. Do NOT add a call site in ralph-build.sh yet; T7 owns every call site.","task_index":1,"total_tasks":7,"attempt":1,"max_attempts":3,"phase":"verifying","verify_pass":null,"last_commit":"8ce1e36","started":1787849696,"updated":1787849795}
