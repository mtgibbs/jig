{"agent":"qwen","pid":660081,"repo":"run-ext","branch":"spec/strategy-conf","spec":"specs/20260828c-strategy-file-extension","task":"T2: update every remaining reference to the old extension, outside the resolution itself. Search the tracked files for strategy filenames written as `<name>.env` — they appear in scripts/loops/README.md, which documents the contract, and in prose and comments under docs/ and scripts/. Rewrite each to the `.conf` form so no tracked file still calls a strategy `<name>.env`. Two of those references are LOAD-BEARING and must be updated or their gates go red the moment the files move: specs/20260825c-executor-binding/verify.sh reads `scripts/loops/build-codex.env` around line 102, and specs/20260828a-exec-container/verify.sh sets `STRAT` to `scripts/loops/build-container.env` around line 24. Update both to the `.conf` name. Do not touch `.env` mentions that are NOT about strategy files: an unrelated reference to a dotenv file elsewhere is a different thing with the same spelling, and rewriting it would be wrong. Do NOT rewrite anything under `.evidence/` — index-*.md and index-*.jsonl are generated records of runs that really did source a file with the old name, and editing them would falsify history. Change no resolution logic; T1 owns that.","task_index":2,"total_tasks":2,"attempt":3,"max_attempts":3,"phase":"done","verify_pass":true,"last_commit":"2186353","started":1787890589,"updated":1787892011}
