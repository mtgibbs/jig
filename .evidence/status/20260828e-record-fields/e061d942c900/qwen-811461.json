{"agent":"qwen","pid":811461,"repo":"run-meta","branch":"spec/record-fields","spec":"specs/20260828e-record-fields","task":"T4: teach specs/20260826a-evidence-replayable/verify.sh to assert VALUES, not just keys. Its AC-7 loop checks each of the seventeen schema fields with `jq -e 'has(\"<field>\")'`, and `has()` is true for an empty string — which is precisely how three empty fields shipped behind a green gate. Keep every existing presence check exactly as it is, and add two value assertions after them: that `outcome` is one of the four declared literals `passed`, `failed`, `noop`, `stillborn`, and that `duration_s` is a non-negative integer. Both must be written so they FAIL on the values that ship today — an empty-string outcome and a null duration — because a check that passes on the current broken record would be no check at all. Preserve the null-vs-0 discipline that file already documents: `verify_rc` and `bytes_patch` are legitimately null in cases the schema names, so do not assert those two are non-null. Touch no other file and no other spec.","task_index":1,"total_tasks":2,"attempt":1,"max_attempts":3,"phase":"verifying","verify_pass":null,"last_commit":"b22bd4d","started":1787898779,"updated":1787898922}
