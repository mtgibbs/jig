{"agent":"qwen","pid":860561,"repo":"run-disp","branch":"spec/harness-dispatch","spec":"specs/20260828f-harness-dispatch","task":"T6: set the pod restart policy in the rendered Job, in scripts/dispatch/dispatcher.py. The Job's pod template spec currently omits `restartPolicy`, and a Kubernetes Job REQUIRES it to be either `Never` or `OnFailure` — without it the API server rejects the Job and it never runs at all. Set it to the string `Never`, alongside the existing pod-level fields. `Never` rather than `OnFailure` because the rendered Job already sets `backoffLimit` to 0: the loop owns its own retries, and letting Kubernetes restart the pod would start a second loop over the same spec. Change nothing else in the rendered object and no other function.","task_index":1,"total_tasks":1,"attempt":1,"max_attempts":3,"phase":"verifying","verify_pass":null,"last_commit":"ab3b46d","started":1787901819,"updated":1787901907}
